When would you want to use a remote repository rather than keeping all your work local?
i will use remote repository if i want to synch between my local repository and the shared one on github.

Why might you want to always pull changes manually rather than having Git automatically stay up-to-date with your remote repository?
if it ull the changes automatically maybe it damage the files that iam working on it  so pull it manually prevent that.

Describe the differences between forks, clones, and branches. When would you use one instead of another?
forks : it's a clone but it's cloned in the github enviroment not locally.
clone : u take a copy of the files and the metadata (.git) locally.
branch : it's a label of a certain commit u and it is saperate from the master branch commits untill u merge them together.

What is the benefit of having a copy of the last known state of the remote stored locally?
it will be usful when fetch the changes from github the merge it with the last known state.

How would you collaborate without using Git or GitHub? What would be easier, and what would be harder?
i think nothing will be easier without git and github since everybody can ruin the master branch, but with pull request he will ask me to review his work and if it good i will response to his request else i will give him a feedback to make his contribution good. 

When would you want to make changes in a separate branch rather than directly in master? What benefits does each approach have?
for many reason the first if i want to develop a new functionalty in the project without delay the master version,
second when i want to do a pull request so i can create a new branch specially for this reason.